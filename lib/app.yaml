dependencies:
  - "@softvisio/core"

services:
  server:
    components:
      proxyServer: true

defaultService: server

components:
  env:
    name: proxy

config:
  nginx:
    apiUrl: ws://nginx:81/api
    proxyId: ~
    serverNames: ~
