dependencies:
  - "@softvisio/core"

services:
  server:
    components:
      proxyServer: true

defaultService: server

components: ~

config:
  nginx:
    proxyId: ~
    apiUrl: ~
    serverNames: ~
